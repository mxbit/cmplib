import React from 'react';
import PropTypes from 'prop-types';
import { SimpleCard } from './simplecard/simplecard';


export const Assembly = ({ clickHandler }) => (
    <article>
        <SimpleCard header={'My data'} handler={() =>{}}></SimpleCard>
    </article>
)
